{
  "name": "lab-l12",
  "version": "1.0.0",
  "description": "Lab L-12: Logger Server App",
  "private": true,
  "workspaces": [
    "frontend",
    "logger-server"
  ],
  "scripts": {
    "lars": "lars",
    "lars:config": "lars config",
    "start": "lars start",
    "build": "cd frontend && npm run build && cd ../logger-server && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@bsv/lars": "^1.5.0",
    "@bsv/sdk": "^1.5.2"
  }
}
